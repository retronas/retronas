- hosts: localhost
  gather_facts: no

  roles:
    - retronas.role.sslcert
  
- ansible.builtin.import_playbook: retronas_system_config.yml
  vars:
    module_name: "sslcert"
    module_state: "present"