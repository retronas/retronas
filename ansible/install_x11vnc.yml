---
# Dependencies

- hosts: localhost
  gather_facts: no

  roles:
    - retronas.role.x11vnc

  vars:
    module_name: "x11vnc"

  tasks:
    - ansible.builtin.import_role:
        name: retronas.role.system-config