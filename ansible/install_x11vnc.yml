---
# Dependencies

- hosts: localhost
  gather_facts: no

  roles:
    - retronas.role.x11vnc

- ansible.builtin.import_playbook: retronas_system_config.yml
  vars:
    module_name: "x11vnc"
    module_state: "present"